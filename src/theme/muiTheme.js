import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#ffb944",
      main: "#ee8902",
      dark: "#b55b00",
    },
    secondary: {
      light: "#6695ff",
      main: "#0268ee",
      dark: "#003fba",
    },
  },
});

export default theme;
